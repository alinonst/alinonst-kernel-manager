# Maintainer: alinonst <alinonst@gmail.com>
pkgname=alinonst-kernel-manager
pkgver=1.1.0
pkgrel=1
pkgdesc="A C++ interactive CLI tool to install any Arch Linux kernel..."
arch=('x86_64')
url="https://github.com/alinonst/alinonst-kernel-manager"
license=('AGPL3')
depends=('gcc-libs' 'glibc' 'pacman')
makedepends=('gcc' 'make')
source=("$pkgname-$pkgver.tar.gz::https://github.com/alinonst/alinonst-kernel-manager/archive/v$pkgver.tar.gz")
sha256sums=('SKIP')  //暂时还没生成
